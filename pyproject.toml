[project]
name = "api-local-tts-clean"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "torch",
    "torchaudio",
    "transformers",
    "scipy"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
